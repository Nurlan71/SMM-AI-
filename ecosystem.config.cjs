/**
 * @description Конфигурационный файл для PM2.
 * PM2 — это менеджер процессов для Node.js, который помогает управлять вашим приложением.
 * 
 * ВАЖНО: Перед запуском вам нужно отредактировать секцию `env`.
 * Замените "ВАШ_GEMINI_API_КЛЮЧ" и "ВАШ_НАДЕЖНЫЙ_ПАРОЛЬ_ОТ_БАЗЫ_ДАННЫХ"
 * на ваши реальные данные.
 */
module.exports = {
  apps : [{
    name   : "smm-ai-backend",
    script : "./smm-ai-backend/index.js",
    watch  : false, // Отключаем автоматический перезапуск при изменении файлов для стабильности
    env    : {
      "NODE_ENV": "production",
      
      // --- Ключ от Google AI Studio ---
      // Вставьте сюда ваш API ключ для Gemini
      "API_KEY": "ВАШ_GEMINI_API_КЛЮЧ",

      // --- Настройки подключения к PostgreSQL ---
      // Эти переменные используются библиотекой `pg` по умолчанию.
      // Вам нужно заменить только PGPASSWORD. Остальные, скорее всего, подойдут.
      "PGHOST": "localhost",
      "PGUSER": "smm_ai_user", // Пользователь, которого вы создали в PostgreSQL
      "PGDATABASE": "smm_ai_db", // База данных, которую вы создали
      "PGPASSWORD": "ВАШ_НАДЕЖНЫЙ_ПАРОЛЬ_ОТ_БАЗЫ_ДАННЫХ",
      "PGPORT": 5432,
    }
  }]
}